## 2025-02-14 - Add id and htmlFor to inputs and labels in Login.jsx
**Learning:** Found several input elements missing associated `htmlFor` and `id` linking for labels. This hurts keyboard navigation and screen reader support.
**Action:** Always link form labels to their respective inputs using matching `htmlFor` and `id` values.